---
phase: 02-login-page
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/globals.css
  - app/layout.tsx
autonomous: true
requirements:
  - LOGIN-01

must_haves:
  truths:
    - "Quicksand font renders on every page (not Geist or Arial fallback)"
    - "Custom Tailwind utilities bg-primary, text-on-primary, text-display, font-display, rounded-full, p-md produce expected styles"
    - "Material Symbols Outlined icon font loads and renders icons via ligature text (e.g. pets, arrow_forward)"
    - "html element carries a class containing --font-quicksand (injected by next/font .variable)"
    - "Page title is Pawnder, description is Find your perfect furry match"
  artifacts:
    - path: "app/globals.css"
      provides: "Full @theme design token block (63 tokens) + Material Symbols @import"
      contains: "--color-primary: #9b4500"
    - path: "app/layout.tsx"
      provides: "Quicksand font + Pawnder metadata"
      exports: ["RootLayout"]
  key_links:
    - from: "app/layout.tsx"
      to: "app/globals.css"
      via: "import './globals.css'"
      pattern: "import.*globals\\.css"
    - from: "--font-quicksand (injected by next/font on html)"
      to: "--font-display: var(--font-quicksand) in @theme"
      via: "CSS variable reference"
      pattern: "var\\(--font-quicksand\\)"
---

## Phase Goal

**As a** workshop participant, **I want to** see the Pawnder brand identity render correctly on every page, **so that** the app looks intentional rather than like a boilerplate Next.js app.

Note: ROADMAP Phase 2 Goal is capability-shaped, not in canonical `As a / I want to / so that` format. This story is derived from the goal. To establish a canonical story, run `/gsd mvp-phase 2`.

Note on plan structure: Plan 02-01 is a horizontal foundation layer (design tokens + font) rather than a vertical user slice. This is justified because design tokens must exist before any branded UI can render — building login with default styles and re-skinning afterward would be more rework, not less. Plan 02-02 delivers the first observable user capability.

<objective>
Replace boilerplate globals.css and layout.tsx with the Pawnder design foundation: full Tailwind v4 @theme token block (63 tokens) and Quicksand font loading. This makes all design-system utility classes (bg-primary, text-display, rounded-full, etc.) available to every subsequent page.

Purpose: Every Phase 2 and Phase 3 page depends on these tokens resolving correctly. Without this plan, the login page has no brand identity and Phase 3 swipe UI cannot use design-system classes.
Output: app/globals.css (full @theme), app/layout.tsx (Quicksand + Pawnder metadata)
</objective>

<execution_context>
@/Users/c.ptk/.claude/get-shit-done/workflows/execute-plan.md
@/Users/c.ptk/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace globals.css with full Tailwind v4 @theme design tokens</name>
  <files>app/globals.css</files>

  <read_first>
    - app/globals.css (current state — full replacement needed; contains Geist vars + dark mode)
    - .planning/phases/02-login-page/02-RESEARCH.md Pattern 4 (lines 268–398) — authoritative token block
    - .planning/phases/02-login-page/02-PATTERNS.md "app/globals.css" section — ordering rules
    - design/pawnder_login_desktop/code.html lines 22–107 — Tailwind config that tokens are translated from
  </read_first>

  <action>
Completely replace app/globals.css. The current file contains Geist variables, dark-mode :root vars, @theme inline, and Arial body default — all of these must be removed.

Ordering rules (critical for Tailwind v4):
1. External @import url(...) for Material Symbols MUST come first — before @import "tailwindcss". External @imports placed after Tailwind silently fail in v4.
2. @import "tailwindcss" comes second.
3. @theme block comes after @import "tailwindcss".
4. No :root variables, no @theme inline, no dark-mode media query, no body font override.

Write the file with this exact content:

```css
@import url("https://fonts.googleapis.com/css2?family=Material+Symbols+Outlined:opsz,wght,FILL,GRAD@20..48,100..700,0..1,-50..200&display=swap");

@import "tailwindcss";

@theme {
  --color-primary: #9b4500;
  --color-on-primary: #ffffff;
  --color-primary-container: #ff8c42;
  --color-on-primary-container: #6a2d00;
  --color-primary-fixed: #ffdbc9;
  --color-primary-fixed-dim: #ffb68d;
  --color-on-primary-fixed: #331200;
  --color-on-primary-fixed-variant: #763300;
  --color-surface-tint: #9b4500;
  --color-inverse-primary: #ffb68d;
  --color-secondary: #665d50;
  --color-on-secondary: #ffffff;
  --color-secondary-container: #ede1d0;
  --color-on-secondary-container: #6c6356;
  --color-secondary-fixed: #ede1d0;
  --color-secondary-fixed-dim: #d1c5b5;
  --color-on-secondary-fixed: #211b11;
  --color-on-secondary-fixed-variant: #4d463a;
  --color-tertiary: #3c6a00;
  --color-on-tertiary: #ffffff;
  --color-tertiary-container: #83ba48;
  --color-on-tertiary-container: #274700;
  --color-tertiary-fixed: #b8f47a;
  --color-tertiary-fixed-dim: #9dd761;
  --color-on-tertiary-fixed: #0e2000;
  --color-on-tertiary-fixed-variant: #2c5000;
  --color-error: #ba1a1a;
  --color-on-error: #ffffff;
  --color-error-container: #ffdad6;
  --color-on-error-container: #93000a;
  --color-surface: #fbf9f8;
  --color-surface-dim: #dcd9d9;
  --color-surface-bright: #fbf9f8;
  --color-surface-container-lowest: #ffffff;
  --color-surface-container-low: #f6f3f2;
  --color-surface-container: #f0eded;
  --color-surface-container-high: #eae8e7;
  --color-surface-container-highest: #e4e2e1;
  --color-surface-variant: #e4e2e1;
  --color-on-surface: #1b1c1c;
  --color-on-surface-variant: #564338;
  --color-inverse-surface: #303030;
  --color-inverse-on-surface: #f3f0f0;
  --color-outline: #897266;
  --color-outline-variant: #ddc1b3;
  --color-background: #fbf9f8;
  --color-on-background: #1b1c1c;
  --spacing-xs: 4px;
  --spacing-base: 8px;
  --spacing-sm: 12px;
  --spacing-md: 24px;
  --spacing-lg: 40px;
  --spacing-xl: 64px;
  --spacing-container-padding: 20px;
  --spacing-card-gutter: 16px;
  --radius-DEFAULT: 0.25rem;
  --radius-lg: 0.5rem;
  --radius-xl: 0.75rem;
  --radius-2xl: 1rem;
  --radius-3xl: 1.5rem;
  --radius-full: 9999px;
  --font-display: var(--font-quicksand), sans-serif;
  --font-headline-lg: var(--font-quicksand), sans-serif;
  --font-headline-md: var(--font-quicksand), sans-serif;
  --font-body-lg: var(--font-quicksand), sans-serif;
  --font-body-md: var(--font-quicksand), sans-serif;
  --font-label-lg: var(--font-quicksand), sans-serif;
  --font-label-sm: var(--font-quicksand), sans-serif;
  --text-display: 40px;
  --text-display--line-height: 1.2;
  --text-display--font-weight: 700;
  --text-display--letter-spacing: -0.02em;
  --text-headline-lg: 32px;
  --text-headline-lg--line-height: 1.2;
  --text-headline-lg--font-weight: 700;
  --text-headline-md: 24px;
  --text-headline-md--line-height: 1.3;
  --text-headline-md--font-weight: 600;
  --text-body-lg: 18px;
  --text-body-lg--line-height: 1.6;
  --text-body-lg--font-weight: 500;
  --text-body-md: 16px;
  --text-body-md--line-height: 1.6;
  --text-body-md--font-weight: 400;
  --text-label-lg: 14px;
  --text-label-lg--line-height: 1.2;
  --text-label-lg--font-weight: 600;
  --text-label-lg--letter-spacing: 0.01em;
  --text-label-sm: 12px;
  --text-label-sm--line-height: 1.2;
  --text-label-sm--font-weight: 700;
}

.material-symbols-outlined {
  font-variation-settings: "FILL" 0, "wght" 400, "GRAD" 0, "opsz" 24;
  font-family: "Material Symbols Outlined";
}
.material-symbols-outlined.fill {
  font-variation-settings: "FILL" 1, "wght" 400, "GRAD" 0, "opsz" 24;
}
```

Why the font tokens work: next/font/google injects --font-quicksand as a CSS custom property on the html element via quicksand.variable in layout.tsx. All --font-* tokens reference var(--font-quicksand) so they resolve through that injected CSS var. Using .variable (not .className) in layout.tsx is what makes this chain work — do not change it.

String conventions: use double quotes throughout (project convention — all .ts files use double quotes).
  </action>

  <verify>
    <automated>
      grep -cE '^@import "tailwindcss";$' app/globals.css
      grep -c -- '--color-primary: #9b4500;' app/globals.css
      grep -c 'Material.Symbols.Outlined' app/globals.css
      grep -cE '^  --text-display: 40px;$' app/globals.css
      grep -cE '^  --font-display: var\(--font-quicksand\)' app/globals.css
      npm test
    </automated>
  </verify>

  <acceptance_criteria>
    - `grep -cE '^@import "tailwindcss";$' app/globals.css` returns 1 (anchored — the comment about tailwindcss ordering does not match)
    - `grep -c -- '--color-primary: #9b4500;' app/globals.css` returns 1
    - `grep -c 'Material.Symbols.Outlined' app/globals.css` returns at least 2 (once in @import URL, once in .material-symbols-outlined selector)
    - `grep -cE '^  --text-display: 40px;$' app/globals.css` returns 1 (anchored to indented property line)
    - `grep -cE '^  --font-display: var\(--font-quicksand\)' app/globals.css` returns 1
    - `grep -c -- '--spacing-md: 24px;' app/globals.css` returns 1
    - `grep -c -- '--radius-full: 9999px;' app/globals.css` returns 1
    - `grep -c 'Geist' app/globals.css` returns 0 (boilerplate removed)
    - `grep -c 'Arial' app/globals.css` returns 0 (body font override removed)
    - `npm test` passes (all 15 existing API tests green)
  </acceptance_criteria>

  <done>app/globals.css contains the full @theme block with all 63 design tokens, Material Symbols @import appears before @import "tailwindcss", no Geist or Arial remnants, all 15 existing tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: Replace layout.tsx with Quicksand font and Pawnder metadata</name>
  <files>app/layout.tsx</files>

  <read_first>
    - app/layout.tsx (current state — contains Geist/Geist_Mono imports and Create Next App metadata)
    - .planning/phases/02-login-page/02-RESEARCH.md Pattern 5 (lines 407–443) — authoritative layout.tsx pattern
    - .planning/phases/02-login-page/02-PATTERNS.md "app/layout.tsx" section — why .variable not .className
  </read_first>

  <action>
Completely replace app/layout.tsx. The current file imports Geist and Geist_Mono — remove both. Replace with Quicksand loaded via next/font/google.

Why use .variable and not .className on html: .variable injects --font-quicksand as a CSS custom property on the html element. The @theme tokens (--font-display: var(--font-quicksand), etc.) reference this CSS var — they require it to be a CSS variable, not a class. Using .className applies the font directly but does not expose a CSS variable; all font-family tokens would fall through to sans-serif. Never change .variable to .className.

Write the file with this exact content:

```tsx
import type { Metadata } from "next";
import { Quicksand } from "next/font/google";
import "./globals.css";

const quicksand = Quicksand({
  subsets: ["latin"],
  weight: ["400", "500", "600", "700"],
  variable: "--font-quicksand",
});

export const metadata: Metadata = {
  title: "Pawnder",
  description: "Find your perfect furry match",
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en" className={quicksand.variable}>
      <body>{children}</body>
    </html>
  );
}
```

Key constraints:
- quicksand.variable on html — NOT quicksand.className (see explanation above)
- Weight array must be string values: ["400", "500", "600", "700"] — all four weights needed for font-weight tokens
- No Geist, no Geist_Mono imports
- No className on body — pages control their own layout
- Material Symbols is loaded via CSS @import in globals.css — no font import needed here
- Double quotes for all string literals (project convention)
  </action>

  <verify>
    <automated>
      grep -cE 'className=\{quicksand\.variable\}' app/layout.tsx
      grep -c 'quicksand.className' app/layout.tsx
      grep -c '"Pawnder"' app/layout.tsx
      grep -c 'Geist' app/layout.tsx
      npm test
    </automated>
  </verify>

  <acceptance_criteria>
    - `grep -cE 'className=\{quicksand\.variable\}' app/layout.tsx` returns 1 (anchored to JSX attribute)
    - `grep -c 'quicksand.className' app/layout.tsx` returns 0 (wrong pattern absent)
    - `grep -c '"Pawnder"' app/layout.tsx` returns 1
    - `grep -c '"Find your perfect furry match"' app/layout.tsx` returns 1
    - `grep -c 'Geist' app/layout.tsx` returns 0 (boilerplate removed)
    - `grep -c 'variable: "--font-quicksand"' app/layout.tsx` returns 1
    - `grep -c 'Quicksand' app/layout.tsx` returns at least 2 (import + constructor call)
    - `npm test` passes (all 15 existing API tests green)
  </acceptance_criteria>

  <done>app/layout.tsx loads Quicksand via next/font/google with .variable applied to html, metadata title is "Pawnder", no Geist remnants, all 15 existing tests pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CDN → browser | Material Symbols font loaded from Google Fonts CDN; no user data crosses this boundary |
| next/font → filesystem | Quicksand self-hosted at build time; eliminates external request at render |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Spoofing | Material Symbols CDN @import | accept | Font-only CDN load; no script execution; no user data sent; low value target |
| T-02-02 | Tampering | @theme token values in globals.css | accept | CSS is static; no runtime mutation; values set at build time |
| T-02-03 | Information Disclosure | next/font/google self-hosting | mitigate (already mitigated) | next/font self-hosts Quicksand — eliminates Google tracking on font requests; no user IP sent to Google |
</threat_model>

<verification>
After both tasks:
1. `npm run dev` starts without TypeScript errors
2. Visit http://localhost:3000 in browser — page loads (boilerplate pages redirect may 404 until Plan 02-02 — that is expected)
3. Browser DevTools → Elements → html element has a class like __variable_XXXXXX (injected by next/font)
4. Browser DevTools → Computed → verify --font-quicksand CSS variable is defined on html
</verification>

<success_criteria>
- app/globals.css: full @theme block, 63 tokens, Material Symbols @import before @import "tailwindcss", zero Geist/Arial remnants
- app/layout.tsx: Quicksand with .variable on html, title "Pawnder", zero Geist remnants
- All 15 existing API tests remain green
- --font-quicksand CSS variable resolves in browser
</success_criteria>

<output>
After completion, create `.planning/phases/02-login-page/02-01-SUMMARY.md`
</output>
